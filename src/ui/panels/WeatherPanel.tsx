export function WeatherPanel() {
  return (
    <div className="panel">
      <h2>Weather</h2>
      <p className="empty">
        Coming soon — we still need to pick a weather data provider (e.g. Open-Meteo, NWS, or
        OpenWeatherMap) before wiring this up.
      </p>
    </div>
  )
}
